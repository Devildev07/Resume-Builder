import { CarousalNextDirective } from './carousal-next.directive';

describe('CarousalNextDirective', () => {
  it('should create an instance', () => {
    const directive = new CarousalNextDirective();
    expect(directive).toBeTruthy();
  });
});
