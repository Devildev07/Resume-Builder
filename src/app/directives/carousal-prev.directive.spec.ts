import { CarousalPrevDirective } from './carousal-prev.directive';

describe('CarousalPrevDirective', () => {
  it('should create an instance', () => {
    const directive = new CarousalPrevDirective();
    expect(directive).toBeTruthy();
  });
});
